export const enum LanguageEnum {
  ENGLISH = "en",
  FRENCH = "fr",
}

export const RESTRICTED_ROUTES = ["/login", "/sign-up"];
export const PRIVATE_ROUTES = ["/profile"];

export const ACCESS_TOKEN = "access_token";
